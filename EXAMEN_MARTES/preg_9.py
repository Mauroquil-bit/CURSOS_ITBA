# Un programa de Python que utiliza funciones de matplotlib 
# lanzó un error cuando se intentó ejecutar 
# ¿Cuál es la causa del error?
#

# ValueError
# 13
# 14
# 15 plt.plot(x,y, 'R--', label='IEEE')
# 16 plt.legend()
# 17 plt.show()

# La línea 15 tiene un error de sintaxis.

# 103   else:
# 104       raise ValueError("Unrecognized character %c in format string" % c)
# 105
# 106 if lineStyles is None and marker is None:

# ValueError: Unrecognized character R in format string

# La línea 15 tiene un error de sintaxis.

# 15 plt.plot(x,y, 'R--', label='IEEE')
# 16 plt.legend()
# 17 plt.show()

# ¿Que significa raise ValueError("Unrecognized character %c in format string" % c)?
# raise ValueError("Unrecognized character %c in format string" % c)
# lanza un error de tipo ValueError con el mensaje "Unrecognized character %c in format string" % c.

# ¿Que significa lineStyles is None and marker is None?
# lineStyles is None and marker is None
# es una condición que se evalúa como True si lineStyles es None y marker es None.

# ¿Que significa if lineStyles is None and marker is None:
# if lineStyles is None and marker is None:
# es una condición que se evalúa como True si lineStyles es None y marker es None.

# ¿Que significa "Unrecognized character %c in format string" % c?
# "Unrecognized character %c in format string" % c
# es un mensaje que se muestra cuando se lanza un error de tipo ValueError.

# ¿Que significa raise ValueError("Unrecognized character %c in format string" % c)?
# raise ValueError("Unrecognized character %c in format string" % c)
# lanza un error de tipo ValueError con el mensaje "Unrecognized character %c in format string" % c.

# ¿Que significa lineStyles is None and marker is None?
# lineStyles is None and marker is None


